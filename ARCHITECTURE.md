# Gym Tracker by ARTYZ — สรุปสถาปัตยกรรมสำหรับให้ AI ช่วยแก้โค้ด

> เอกสารนี้ทำไว้เพื่อ **คัดลอกไปวางในแชท Claude ใหม่** แล้วบอกว่าอยากแก้อะไร
> Claude จะเข้าใจโครงสร้างทันทีโดยไม่ต้องอ่านโค้ดทั้งโปรเจกต์

---

## 1. ภาพรวม

เว็บแอปตารางฝึกเวทภาษาไทย · **React 19 + TypeScript + Vite 6 + Tailwind 3**
mobile-first (ออกแบบสำหรับ iPhone Safari / PWA) · ธีมกระจกมืด accent สีฟ้า cyan

**สำคัญที่สุด: ไม่มี backend ไม่มีฐานข้อมูล ไม่มี API**
ข้อมูลผู้ใช้ทั้งหมดอยู่ใน `localStorage` คีย์เดียวชื่อ `gymtracker_v1`
runtime dependency มีแค่ `react` + `react-dom` (จงใจให้ bundle เล็ก ~400 KB)

### สถาปัตยกรรม state (เรียบง่ายมาก อย่าเพิ่ม library)
- state ทั้งแอปอยู่ที่ `App.tsx` **ตัวเดียว** เป็น `useState<Data>`
- ส่งลงลูกผ่าน `AppContext` → ใช้ด้วย `useApp()` ได้ทุก component
- แก้ข้อมูลด้วย `update(draft => { ...แก้ draft... })` — ข้างในทำ `structuredClone` ให้แล้ว
- **ไม่มี Redux / Zustand / React Query — ห้ามเพิ่ม**
- **ไม่มี router** — สลับแท็บด้วย `useState<TabId>` ใน App.tsx (5 แท็บ)
- ทุกครั้งที่ `data` เปลี่ยน → `useEffect` เซฟลง localStorage อัตโนมัติ

```tsx
// แพตเทิร์นการใช้งานใน component ทุกตัว
const { data, update, toast, rest, goTab } = useApp();
update((d) => { d.exercises.push(newEx); });   // แก้ข้อมูล
toast("บันทึกแล้ว", true);                      // แจ้งเตือน (true = เรืองแสง)
rest.current?.start(90);                        // เริ่มจับเวลาพัก 90 วิ
goTab("analyze");                               // สลับแท็บ
```

---

## 2. ไฟล์ทั้งหมด และหน้าที่ของแต่ละไฟล์

### แกนหลัก
| ไฟล์ | หน้าที่ |
|---|---|
| `src/App.tsx` | shell ทั้งแอป · state ตัวเดียว · bottom nav 5 แท็บ · header · toast |
| `src/AppContext.tsx` | นิยาม context (`data`, `update`, `toast`, `rest`, `goTab`) + hook `useApp()` |
| `src/index.css` | Tailwind + CSS variables ทั้งหมด (`--acc`, `--ink`, `--dim`, `--edge-hi` ฯลฯ) |
| `src/main.tsx` | entry point |

### ชั้นข้อมูล (`src/lib/`)
| ไฟล์ | หน้าที่ |
|---|---|
| **`store.ts`** | **หัวใจ** — data model ทุก interface + อ่าน/เขียน localStorage + migration + ระบบสลับท่าชั่วคราว (swaps) + ประวัติแบบ archive |
| `muscles.ts` | taxonomy กลางทั้งหมด — 13 กลุ่มกล้ามเนื้อ, 9 แพทเทิร์นการเคลื่อนไหว, 11 ชนิดอุปกรณ์, ระดับความล้า, เป้าปริมาณตามประสบการณ์, ค่าคงที่ทางสรีรวิทยา (48 ชม.ฟื้นตัว ฯลฯ) |
| `exerciseDB.ts` | คลังท่า ~126 ท่า แต่ละท่ามี metadata ครบ (อุปกรณ์, กล้ามหลัก/รอง, แพทเทิร์น, ความล้า, เซต/เรปแนะนำ, ข้อห้ามเมื่อบาดเจ็บ, tip) + ระบบ **tier S/A/B** + ค้นหา |
| `profile.ts` | ตัวกลางอ่านค่า default — เช่น `getDayEquip(d, "mon")` ถ้าผู้ใช้ไม่ตั้งจะคืน "มีครบทุกอย่าง" |
| **`analyzer.ts`** | **เอนจินวิเคราะห์ + แนะนำ** (ไฟล์ใหญ่สุด ~66 KB) ดูรายละเอียดหัวข้อ 4 |
| `blueprint.ts` | โครงตารางมาตรฐาน (PPL 3 วัน / Upper-Lower 4 วัน / 5 วัน) + ช่องท่าของแต่ละประเภทวัน — ใช้ตอนสร้างโปรแกรมทั้งชุด |
| `progression.ts` | เป้าน้ำหนักครั้งหน้า (`suggestTarget`) · warm-up ramp · คำนวณแผ่นน้ำหนัก · เวลาพักแนะนำ (`suggestRest`) |
| `streak.ts` | สตรีค + heatmap (วันพักตามโปรแกรมไม่ตัดสตรีค) |
| `forecast.ts` | พยากรณ์ PR ด้วย linear regression จาก 4+ เซสชันล่าสุด |
| `share.ts` | วาดการ์ดสรุปสัปดาห์บน canvas 1080×1350 → Web Share API หรือดาวน์โหลด PNG |
| `programParser.ts` | แปลงข้อความโปรแกรมที่วางมาทีเดียวเป็นรายการท่า (รับทั้งไทย/อังกฤษ, เซตxเรป, น้ำหนัก, วินาที, AMRAP) |
| `programLink.ts` | เข้ารหัส/ถอดรหัสโปรแกรมเป็นลิงก์แชร์ (`?p=...`) |
| `sound.ts` | เสียงสังเคราะห์ด้วย Web Audio (ติ๊กเซต/ครบท่า/PR/พักครบ) — ปิดได้ |
| `haptics.ts` | รูปแบบการสั่น (iOS Safari ไม่รองรับ เงียบเฉยๆ ไม่พัง) |
| `accent.ts` | ระบบสีธีม — เซ็ตค่าลง CSS variable `--acc` |
| `edition.ts` | อ่าน `VITE_EDITION` → บอกว่าเป็นรุ่น personal หรือ pro |
| `premium.ts` | ตรรกะสิทธิ์ใช้งาน — รุ่น personal เปิดหมดตลอด, รุ่น pro ทดลอง 30 วัน |
| `license.ts` | ตรวจรหัสปลดล็อกในเครื่อง (fnv1a checksum) + ราคา |

### ชั้น UI (`src/components/`)
| ไฟล์ | หน้าที่ |
|---|---|
| `TodayView.tsx` | **แท็บ "วันนี้"** — ติ๊กเซต, ใส่น้ำหนัก/เรป, เป้าครั้งหน้า, warm-up, สลับท่าชั่วคราว, เริ่มจับเวลาพัก |
| `ProgramView.tsx` | **แท็บ "ตาราง"** — ดูภาพรวมทั้งสัปดาห์ |
| `AnalyzerView.tsx` | **แท็บ "วิเคราะห์"** — วงแหวนคะแนนคู่, ปริมาณต่อกล้ามเนื้อ, ภาระต่อวัน, สมดุลแพทเทิร์น, **การ์ดคำแนะนำที่กดใช้ได้เลย** |
| `ProgressView.tsx` | **แท็บ "ก้าวหน้า"** — กราฟความก้าวหน้าแต่ละท่า + พยากรณ์ PR |
| `ManageView.tsx` | **แท็บ "จัดการ"** (ไฟล์ใหญ่ ~30 KB) — เพิ่ม/แก้/ลบท่า, ตั้งชื่อวัน, การ์ดย่อยหลายใบ |
| `RestTimer.tsx` | ตัวจับเวลาพักลอย ลากขยับได้ จำตำแหน่ง — เปิดผ่าน ref (`rest.current?.start()`) |
| `ExercisePicker.tsx` | ช่องค้นหาท่าจากคลัง 126 ท่า |
| `ProfileCard.tsx` | export 2 ตัว: `ProfileCard` (ประสบการณ์/เป้าหมาย/บาดเจ็บ — **ปัจจุบันถอดออกจาก UI แล้ว**) และ `DayEquipmentCard` (เลือกอุปกรณ์รายวัน — ยังใช้อยู่) |
| `ImportProgramCard.tsx` | วางข้อความโปรแกรมทีเดียว |
| `SavedProgramsCard.tsx` | บันทึก/สลับ/ลบโปรแกรมหลายชุด |
| `IncomingProgram.tsx` | หน้าต่างรับโปรแกรมจากลิงก์ที่แชร์มา |
| `StreakCard.tsx` | สตรีค + heatmap |
| `BodyCompCard.tsx` | น้ำหนักตัว + ผลสแกนร่างกาย (Inbody) |
| `UpgradeCard.tsx` | การ์ดขาย/ใส่รหัสปลดล็อก (แสดงเฉพาะรุ่น pro) |
| `ui.tsx` | คอมโพเนนต์ UI ที่ใช้ซ้ำ |

---

## 3. Data Model (`store.ts`) — สิ่งที่ต้องระวังที่สุด

```ts
interface Data {
  dayLabels: Record<DayKey, string>;       // ชื่อวัน เช่น { mon: "Push", wed: "Pull" }
  exercises: Exercise[];                   // ท่าทั้งหมด แต่ละท่าผูกกับวัน (ex.day)
  history: Record<string, SessionLog[]>;   // key = exercise.id → ประวัติทุกเซสชัน
  bodyweight: BwEntry[];
  bodyScans: BodyScan[];
  settings: Settings;
  savedPrograms?: SavedProgram[];          // โปรแกรมที่บันทึกไว้หลายชุด
  historyArchive?: Record<string, SessionLog[]>;  // ประวัติเก็บตาม "ชื่อท่า" — ลบท่าแล้วเพิ่มกลับ ประวัติกลับมา
  swaps?: Swaps;                           // สลับท่าชั่วคราวเฉพาะวันนี้ (วันเปลี่ยน = กลับท่าเดิมเอง)
  profile?: Profile;                       // ประสบการณ์/เป้าหมาย/บาดเจ็บ
  constraints?: Constraints;               // เพดานเวลา/เซตต่อวัน
  dayEquip?: Partial<Record<DayKey, EquipTag[]>>;  // อุปกรณ์แยกรายวัน (ยิมบางวัน บ้านบางวัน)
}

interface Exercise {
  id: string; name: string; day: DayKey;
  type: "weight" | "bodyweight" | "time";
  sets: number; rmin: number; rmax: number;
  inc?: number; unit?: string; amrap?: boolean; order?: number;
  restSec?: number;    // ผู้ใช้ตั้งเวลาพักเอง — ไม่มี = ใช้ค่าที่ระบบแนะนำ
  machine?: boolean;   // ท่าเครื่อง (น้ำหนักรวม ไม่ใช่ต่อข้าง ไม่มี warm-up ramp)
}
```

### ⚠️ กฎเหล็กเรื่องข้อมูล (ถ้าพลาด = ประวัติผู้ใช้หายถาวร กู้ไม่ได้)
1. **ฟิลด์ใหม่ทุกตัวต้องเป็น optional (`?`) เสมอ** และตีความ `undefined` = ค่าเดิมของระบบ
   (ดูแพตเทิร์นจาก `soundEnabled?`, `smartRest?`, `machine?`)
2. **migration ต้องบวกเพิ่มเท่านั้น** — ห้ามลบ ห้าม rename คีย์เก่า
3. **ห้ามเปลี่ยนคีย์ `gymtracker_v1` เด็ดขาด**
4. ไม่มี backup บนคลาวด์ ไม่มี undo — ข้อมูลจริงมีที่เดียวคือเครื่องผู้ใช้

---

## 4. เอนจินวิเคราะห์ (`analyzer.ts`) — ระบบที่ซับซ้อนสุด

### หลักการออกแบบหลัก
> **ตรวจข้อจำกัดก่อน แล้วค่อยหาช่องว่าง ไม่ใช่ทางกลับกัน**

คำแนะนำทุกข้อต้องผ่าน **ตัวกรองบังคับ 5 ด่าน** (`checkFilters`) ก่อนถูกแสดง:
0. อาการบาดเจ็บที่ผู้ใช้แจ้ง
1. **อุปกรณ์ของวันนั้น** — ต้องมีครบทุกชิ้นที่ท่าต้องใช้
2. เพดานเซสชัน (จำนวนเซต **และ** เวลาโดยประมาณ)
3. เพดานกล้ามเนื้อต่อวัน (สูงสุด 10 เซตต่อมัดต่อวัน)
4. ระยะห่างการฟื้นตัว (กล้ามเดิมโดนหนักต้องห่าง ≥ 48 ชม. คิดแบบสัปดาห์วนกลับ)

ไม่ผ่านด่านไหน = **ตัดทิ้ง ไม่แสดงเลย** แล้วไปอธิบายใน `blockedInsights` ว่าทำไมทำไม่ได้และต้องแก้ที่ต้นเหตุอะไร
(เหตุผล: เสนอ "เพิ่มบาร์เบลฮิปทรัสวันศุกร์" ให้คนที่ศุกร์เล่นที่บ้านไม่มีบาร์เบล = ผู้ใช้เลิกเชื่อระบบทั้งหมด)

### ระบบคะแนน 2 ชั้น
- **`execution`** = คะแนนที่ตารางนี้ทำได้จริงตอนนี้ (0–100)
- **`ceiling`** = เพดานสูงสุดที่ข้อจำกัดของผู้ใช้อนุญาต (เช่นเข้ายิมได้แค่ 2 วัน เพดานจะต่ำกว่า 100)

คะแนนแบ่ง 5 หมวดถ่วงน้ำหนัก:
| หมวด | น้ำหนัก | ดูอะไร |
|---|---|---|
| `volume` | 40% | แต่ละกล้ามเนื้อได้เซต/สัปดาห์ อยู่ในช่วงเป้าไหม (นับแบบเศษส่วน: กล้ามหลัก 1.0 กล้ามรอง 0.5) |
| `patterns` | 20% | สมดุลแพทเทิร์น — ดึงเข้าตัวต้องไม่น้อยกว่าดันออกหน้า, มีท่าดึงลงล่าง, มีท่าบานพับสะโพก |
| `recovery` | 20% | กล้ามเดิมไม่โดนหนักในวันที่ห่างกัน < 48 ชม. + ไม่ฝึกติดกันเกิน 3 วัน |
| `sessionCap` | 10% | ไม่มีวันไหนเกินเพดานเซต/เวลา |
| `order` | 10% | ลำดับท่าถูกหลัก — ท่าล้าสูงมาก่อน, core/น่องปิดท้าย |

### ชนิดคำแนะนำ (`RecKind`) — ทุกชนิดกดใช้ได้จริง
`buildProgram` (สร้างทั้งโปรแกรมจากโครงมาตรฐาน) · `addDay` (เปิดวันฝึกใหม่ทั้งวัน) · `add` (เพิ่มท่า) · `increaseSets` · `reduceSets` · `reorder` (จัดลำดับท่าใหม่) · `restDay` (ย้ายวันฝึก) · `splitDay` (แยกวันที่อัดเกิน) · `moveExercise` · `removeExercise`

### กลไกสำคัญ: **จำลองกดจริงก่อนเสนอ**
ทุกคำแนะนำถูกสร้างโดย `predictedAnalysis()` = clone ข้อมูล → `applyRecommendation()` → วิเคราะห์ใหม่ → เทียบคะแนน
**ไม่ใช่การเดา** — ระบบเลือกตัวเลือกที่คะแนนขึ้นจริง และเทียบ 3 ทางเลือก (เพิ่มท่าวันเดิม / เพิ่มเซต / เปิดวันใหม่) ด้วยเกณฑ์เดียวกัน (เพดานก่อน คะแนนจริงรองลงมา)

เพดานวันฝึกตายตัวที่ **5 วัน/สัปดาห์** (`MAX_TRAINING_DAYS` ใน blueprint.ts) — กันคำแนะนำลามจนไม่มีวันพัก

### ฟังก์ชันสำคัญที่ควรรู้จัก
```ts
analyzeProgram(data): Analysis              // วิเคราะห์ทั้งตาราง คืนคะแนน+ปัญหา+สถิติ
buildRecommendations(data, analysis): Recommendation[]   // สร้างคำแนะนำ (สูงสุด 3 ข้อ)
applyRecommendation(draft, rec)             // ลงมือแก้ตารางตามคำแนะนำ
checkFilters(data, tpl, day, sets): FilterVerdict         // ตัวกรอง 5 ด่าน
muscleMap(name): MuscleHit[]                // ชื่อท่า → กล้ามเนื้อที่โดน + น้ำหนัก
minGapHours(a, b): number                   // ระยะห่างวันแบบสัปดาห์วนกลับ (เสาร์→จันทร์ = 48 ไม่ใช่ 120)
reorderDay(draft, day)                      // จัดลำดับท่าในวันนั้นให้ถูกหลัก
```

---

## 5. ระบบสองรุ่นจากโค้ดชุดเดียว

`VITE_EDITION` กำหนดรุ่นตอน build · **ค่าเริ่มต้นคือ `personal` เสมอ**

| รุ่น | คำสั่ง build | ออกที่ | ต่างกันตรงไหน |
|---|---|---|---|
| **personal** (ของเจ้าของ) | `npm run build` | `dist/` | เปิดทุกฟีเจอร์ ไม่มีช่วงทดลอง ไม่มีรหัส |
| **pro** (ที่ขาย) | `npm run build:pro` | `dist-pro/` | ทดลองฟรี 30 วัน แล้วล็อกฟีเจอร์ "สมองโค้ช" · จ่ายครั้งเดียว 299฿ ปลดล็อกถาวร |

### กฎการล็อกฟีเจอร์
**ห้ามล็อกเด็ดขาด** (ล็อกข้อมูลผู้ใช้ = จับข้อมูลเขาเป็นตัวประกัน):
บันทึกฝึก · ประวัติ · สตรีค · จับเวลาพัก · การ์ดสรุปสัปดาห์ · นำเข้า/ส่งโปรแกรม

**ล็อกได้**: บอกน้ำหนักครั้งหน้า (`suggestTarget`) · วิเคราะห์โปรแกรม · warm-up ramp · พยากรณ์ PR

จุดที่ล็อกอยู่จริงในโค้ด: `TodayView.tsx`, `AnalyzerView.tsx`, `ProgressView.tsx` (เช็คด้วย `isPremium(data)`)

⚠️ โค้ดตรวจสิทธิ์ต้องถูก **tree-shake ออกจาก build รุ่น personal ทั้งหมด** —
ตรวจด้วยการ grep หา `artyz-coach-2026` ใน `dist/assets/*.js` **ต้องไม่เจอ**

⚠️ ข้อจำกัดที่รู้อยู่แล้ว: การตรวจรหัสอยู่ฝั่งเบราว์เซอร์ล้วน ใครเปิด DevTools ก็แกะได้
เป็นการตัดสินใจโดยเจตนา (ยอมแลกกับการไม่ต้องทำ backend) — ถ้าจะปิดช่องนี้จริงต้องย้ายไปตรวจฝั่งเซิร์ฟเวอร์

---

## 6. แนวทางเขียนโค้ดของโปรเจกต์นี้ (ทำตามให้เหมือนของเดิม)

- **คอมเมนต์เป็นภาษาไทย อธิบาย _ทำไม_ ไม่ใช่ _ทำอะไร_** (ดูตัวอย่างใน `store.ts`, `analyzer.ts`)
- UI ภาษาไทยทั้งหมด
- สี**อย่า hardcode** ให้ใช้ CSS variable (`var(--acc)`, `var(--ink)`, `var(--dim)`, `var(--edge-hi)`)
  โดยเฉพาะ `--acc` เพราะผู้ใช้เปลี่ยนสีธีมได้
- คุมความกว้าง `max-w-[520px]` และเผื่อ `env(safe-area-inset-*)` ทุกขอบ (iPhone notch)
- **ระวัง bundle size** — เคยถอด voice logging + Supabase ออกเพื่อลดจาก 510 → 282 KB
  ก่อนเพิ่ม dependency ใหม่ต้องถามเจ้าของก่อน
- ไม่มี test framework — เทสเป็นสคริปต์ `.mjs` ใน `scripts/` bundle ด้วย esbuild แล้วรันบน Node

---

## 7. ชุดทดสอบที่มีอยู่ (ต้องผ่านหมดก่อน commit)

```powershell
$env:PATH = "C:\ARTY\COAD\CLAUDE\gym-tracker\.tools\node-v24.18.0-win-x64;" + $env:PATH

# แต่ละไฟล์รันแบบนี้ (เปลี่ยนชื่อไฟล์)
.\node_modules\.bin\esbuild.cmd scripts/test-engine.mjs --bundle --platform=node --format=esm --outfile=t.mjs; node t.mjs
```

| ไฟล์เทส | ทดสอบอะไร | สถานะ |
|---|---|---|
| `test-engine.mjs` | 8 เคสตามสเปค + ตัวกรอง + ระยะห่างวันแบบวนกลับ | 21/21 |
| `test-followrecs.mjs` | เริ่มจากตารางว่างเปล่า กดตามคำแนะนำ → ต้องถึง 100 | 8/8 |
| `test-existing.mjs` | ตารางเละ 3 แบบ (Push วันเดียว / จัดวันสุ่ม / แขนล้วน) → ต้องถึง 100 | 12/12 |
| `test-equipment.mjs` | ตารางจริงที่อุปกรณ์ต่างกันตามวัน (ยิม/บ้าน) → ต้องถึง 100 | 5/5 |
| `test-logic.mjs` | parser / เวลาพัก / พยากรณ์ | — |

---

## 8. คำสั่งที่ใช้บ่อย

```powershell
# ⚠️ ต้อง set PATH ก่อนทุกครั้ง (เครื่องนี้ใช้ Node แบบ portable ไม่ได้ติดตั้งใน system)
$env:PATH = "C:\ARTY\COAD\CLAUDE\gym-tracker\.tools\node-v24.18.0-win-x64;" + $env:PATH

npm run dev        # dev server
npm run build      # -> dist/     รุ่น personal (typecheck ด้วย)
npm run build:pro  # -> dist-pro/ รุ่นที่ขาย
npm run bundle     # Parcel -> bundle.html ไฟล์เดียว (ไม่มี offline/SW)
npm run license 5  # สร้างรหัสปลดล็อก 5 อัน
```

### Deploy — GitHub Pages อัตโนมัติ
push ขึ้น `main` → GitHub Actions build ทั้งสองรุ่นแล้ว deploy เอง

- รุ่นขาย → https://artyzis.github.io/gym-tracker/
- รุ่นส่วนตัว → https://artyzis.github.io/gym-tracker/me/

ตรวจว่าขึ้นจริงไหม: เทียบ hash ของ `assets/index-*.js` ในหน้าเว็บกับใน `dist/`

---

## 9. ข้อจำกัดแพลตฟอร์ม — **อย่าพยายาม "แก้" สิ่งเหล่านี้ มันแก้ไม่ได้**

- **iOS Safari suspend JS เร็วมาก** เมื่อสลับแอป/ล็อกจอ → ตัวจับเวลาพักจึงคำนวณจาก timestamp
  (ไม่ใช่ setInterval นับถอยหลัง) เพื่อให้แสดงเวลาถูกเสมอเมื่อกลับมา
  ถ้าจะให้แจ้งเตือนแม่น 100% แม้ปิดแอปนาน **ต้องใช้ Web Push + server เท่านั้น**
- `new Notification()` **ใช้ไม่ได้บน iOS** — โค้ด fallback เป็น Service Worker notification
- **Vibration API: iOS Safari ไม่รองรับ** (เงียบเฉยๆ ไม่พัง) Android/Chrome ทำงานปกติ
- `bundle.html` ไฟล์เดียว **ไม่มี** Service Worker/offline — เป็นข้อจำกัดของไฟล์เดี่ยว ไม่ใช่บั๊ก

---

## 10. ตัวอย่างวิธีสั่งงาน Claude

> วางเอกสารนี้ แล้วต่อท้ายด้วยสิ่งที่อยากได้ เช่น:

- "อยากเพิ่มฟีเจอร์ตั้งเป้าน้ำหนักตัวรายเดือน ให้แสดงในแท็บก้าวหน้า"
- "การ์ดคำแนะนำในแท็บวิเคราะห์อยากให้กดแล้วเห็นก่อนว่าจะเปลี่ยนอะไร (preview) ก่อนยืนยัน"
- "อยากเพิ่มท่าใหม่ 20 ท่าเข้าคลัง ต้องแก้ไฟล์ไหนและใส่ metadata อะไรบ้าง"
- "เอนจินวิเคราะห์ตอนนี้เพดานวันฝึก 5 วัน อยากให้ผู้ใช้ตั้งเองได้"
- "อยากเปลี่ยนธีมเป็นโหมดสว่างด้วย ต้องแก้ตรงไหน"

**สิ่งที่ควรบอก Claude ทุกครั้ง:** อยากแก้รุ่นไหน (personal/pro/ทั้งคู่) และห้ามทำข้อมูลผู้ใช้หาย
