# ย้ายรุ่นส่วนตัวไป Cloudflare Pages

รุ่นส่วนตัว (เปิดทุกฟีเจอร์ ไม่มีล็อก) ต้องอยู่**คนละโดเมน**กับรุ่นที่ขาย ไม่ใช่แค่คนละ path

**ทำไมแค่ย้าย path ไม่พอ** — `artyzis.github.io` เป็น origin เดียวกันทุก repo
localStorage จึงแชร์กันหมด เปิดรุ่นขายกับรุ่นส่วนตัวบนเครื่องเดียวกัน = ข้อมูลกองเดียวกัน
และใครเดา path `/me/` เจอก็ใช้รุ่นปลดล็อกได้ฟรี

---

## ⚠️ อ่านก่อนเริ่ม: ข้อมูลจะไม่ตามไปเอง

โดเมนใหม่ = localStorage ใหม่ = **แอปจะว่างเปล่า** ประวัติการฝึกทั้งหมดยังอยู่ที่
`artyzis.github.io` ไม่ได้หายไปไหน แต่ต้องย้ายด้วยมือ

> **สถานะปัจจุบัน (15 ส.ค. 2026):** ย้ายเสร็จและปิด `/me/` แล้ว — เอกสารนี้เก็บไว้เป็น
> บันทึกขั้นตอนและวิธีกู้ข้อมูลเก่าถ้าพบว่าตกหล่น (ดูข้อ 6 ท้ายไฟล์)

---

## ขั้นตอน

### 1. ส่งออกข้อมูลจากเครื่องเดิมก่อน

เปิด https://artyzis.github.io/gym-tracker/ → แท็บ **จัดการ**
→ การ์ด **"ย้ายข้อมูลข้ามเครื่อง"** → กด **สร้างโค้ด** แล้วกด **คัดลอก**

(เดิมข้อนี้ให้เปิด `/me/` ซึ่งปิดไปแล้ว — ใช้ URL ข้างบนแทนได้ตรงๆ เป็น origin เดียวกัน
localStorage จึงเป็นกองเดิม โค้ดที่ได้เหมือนกันทุกอย่าง)
→ **วางเก็บไว้ในโน้ตหรือส่งเข้าไลน์ตัวเอง**

โค้ดนี้คือข้อมูลทั้งหมดของคุณ ถ้าทำหาย = เริ่มใหม่

### 2. เอาค่า 2 ตัวจาก Cloudflare มาใส่ GitHub

**ไม่ต้องสร้าง Pages project และไม่ต้องตั้งค่า build ในหน้าเว็บ** — ทั้งหมดอยู่ใน
`.github/workflows/deploy-personal.yml` แล้ว มันจะสร้าง project ให้เองตอน deploy ครั้งแรก

คุณแค่ต้องเอาค่า 2 ตัวนี้มาใส่ GitHub Secrets:

#### 2.1 Account ID

เข้า https://dash.cloudflare.com → เมนูซ้าย **Workers & Pages**
→ ดูแถบขวามือ หัวข้อ **Account ID** → กดไอคอนคัดลอก

#### 2.2 API Token

1. เข้า https://dash.cloudflare.com/profile/api-tokens
2. **Create Token** → เลื่อนหา **Custom token** → **Get started**
3. ตั้งชื่ออะไรก็ได้ เช่น `rankforge-deploy`
4. **Permissions** ใส่บรรทัดเดียว:

   | | | |
   |---|---|---|
   | Account | Cloudflare Pages | **Edit** |

5. **Continue to summary** → **Create Token**
6. **ก๊อปทันที** — หน้านี้แสดงครั้งเดียว ปิดแล้วดูไม่ได้อีก

> Token นี้ให้สิทธิ์แก้ Pages ในบัญชีคุณ อย่าส่งให้ใครรวมทั้งในแชทนี้
> ถ้าเผลอหลุด เข้าหน้าเดิมแล้วกด **Roll** เพื่อยกเลิกใบเก่าได้ทันที

#### 2.3 ใส่ลง GitHub

เข้า https://github.com/ArtyZis/gym-tracker/settings/secrets/actions
→ **New repository secret** ทีละตัว:

| Name | Secret |
|---|---|
| `CLOUDFLARE_ACCOUNT_ID` | ค่าจากข้อ 2.1 |
| `CLOUDFLARE_API_TOKEN` | ค่าจากข้อ 2.2 |

#### 2.4 สั่งรัน

เข้า https://github.com/ArtyZis/gym-tracker/actions
→ เลือก **Deploy personal to Cloudflare Pages** ทางซ้าย
→ **Run workflow** → **Run workflow**

รอ 2-3 นาที จะได้ URL `https://rankforge-me.pages.dev`

> ถ้ายังไม่ได้ใส่ secret workflow จะข้ามไปเฉยๆ ไม่ขึ้นแดง
> และถ้า build ออกมาเป็นรุ่นที่ขายโดยไม่ตั้งใจ workflow จะหยุดเองก่อน deploy

### 3. นำข้อมูลเข้าเครื่องใหม่

เปิด URL ใหม่ → แท็บ **จัดการ** → การ์ด **"ย้ายข้อมูลข้ามเครื่อง"**
→ วางโค้ดจากขั้นตอนที่ 1 ลงในช่องข้อความ → กดปุ่มสีฟ้าด้านล่างเพื่อกู้คืน

ถ้าขึ้น "โค้ดไม่ถูกต้อง" แปลว่าโค้ดขาดตอนตอนก๊อป ให้กลับไปทำขั้นตอนที่ 1 ใหม่

### 4. เช็คให้ครบก่อนไปต่อ

- [ ] ประวัติการฝึกครบ (ดูแท็บ ก้าวหน้า → heatmap ย้อนหลัง)
- [ ] สตรีคเท่าเดิม
- [ ] สถิติสูงสุดครบ
- [ ] น้ำหนักตัว/แรงค์ถูก
- [ ] ท้ายหน้าจัดการขึ้นเลขเวอร์ชันตรงกับที่ deploy ล่าสุด

### 5. ติดตั้งลงหน้าจอโฮมใหม่

ลบไอคอนเก่าทิ้ง แล้วเพิ่มไอคอนจาก URL ใหม่แทน
(ไอคอนเก่าชี้ไปที่ `/me/` ซึ่งปิดไปแล้ว — กดแล้วจะได้รุ่นขายแทน ไม่ใช่รุ่นส่วนตัว)

### 6. ปิด `/me/` — ✅ ทำแล้วเมื่อ 15 ส.ค. 2026

`.github/workflows/deploy.yml` deploy เฉพาะรุ่นที่ขายแล้ว เว็บที่ขายไม่มีรุ่นปลดล็อก
ซ่อนอยู่ใต้อีกต่อไป

**ประวัติเก่าไม่ได้หายไปกับการปิดนี้** — localStorage ผูกกับ origin ไม่ใช่ path
ของที่เคยบันทึกผ่าน `/me/` ยังอยู่ที่ `artyzis.github.io` และเปิดอ่านได้จาก
`artyzis.github.io/gym-tracker/` (คีย์ `gymtracker_v1` เดียวกัน) ถ้าวันหนึ่งพบว่ายัง
ไม่ได้ย้ายอะไรครบ ให้เข้าที่นั่น → **จัดการ** → **สร้างโค้ด** แล้วเอามากู้คืนที่ Cloudflare
(รุ่นขายจะขึ้นช่วงทดลอง/ให้ใส่รหัส แต่ **บันทึกฝึกกับย้ายข้อมูลไม่เคยถูกล็อก** จึงทำได้เสมอ)

---

## หลังย้ายเสร็จจะเป็นแบบนี้

| | URL | รุ่น | ข้อมูล |
|---|---|---|---|
| ที่ขาย | artyzis.github.io/gym-tracker/ | pro (ทดลอง 30 วัน) | แยกของตัวเอง |
| ของคุณ | rankforge-me.pages.dev | personal (เปิดหมด) | แยกของตัวเอง |

ทั้งสองที่ deploy อัตโนมัติจาก `main` เหมือนกัน push ครั้งเดียวขึ้นทั้งคู่

**ข้อดีที่ได้เพิ่ม:** Cloudflare ตั้ง security header ได้ (`public/_headers`)
ซึ่ง GitHub Pages ทำไม่ได้ — รุ่นส่วนตัวจะได้ CSP กัน XSS ครบกว่าเว็บที่ขายด้วยซ้ำ
