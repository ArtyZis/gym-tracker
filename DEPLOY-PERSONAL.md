# ย้ายรุ่นส่วนตัวไป Cloudflare Pages

รุ่นส่วนตัว (เปิดทุกฟีเจอร์ ไม่มีล็อก) ต้องอยู่**คนละโดเมน**กับรุ่นที่ขาย ไม่ใช่แค่คนละ path

**ทำไมแค่ย้าย path ไม่พอ** — `artyzis.github.io` เป็น origin เดียวกันทุก repo
localStorage จึงแชร์กันหมด เปิดรุ่นขายกับรุ่นส่วนตัวบนเครื่องเดียวกัน = ข้อมูลกองเดียวกัน
และใครเดา path `/me/` เจอก็ใช้รุ่นปลดล็อกได้ฟรี

---

## ⚠️ อ่านก่อนเริ่ม: ข้อมูลจะไม่ตามไปเอง

โดเมนใหม่ = localStorage ใหม่ = **แอปจะว่างเปล่า** ประวัติการฝึกทั้งหมดยังอยู่ที่
`artyzis.github.io` ไม่ได้หายไปไหน แต่ต้องย้ายด้วยมือ

**ห้ามลบ `/me/` ออกจาก GitHub Pages จนกว่าจะย้ายข้อมูลเสร็จและเช็คแล้วว่าครบ**

---

## ขั้นตอน

### 1. ส่งออกข้อมูลจากเครื่องเดิมก่อน

เปิด https://artyzis.github.io/gym-tracker/me/ → แท็บ **จัดการ**
→ การ์ด **"ย้ายข้อมูลข้ามเครื่อง"** → กด **สร้างโค้ด** แล้วกด **คัดลอก**
→ **วางเก็บไว้ในโน้ตหรือส่งเข้าไลน์ตัวเอง**

โค้ดนี้คือข้อมูลทั้งหมดของคุณ ถ้าทำหาย = เริ่มใหม่

### 2. สร้าง Cloudflare Pages

1. สมัคร https://dash.cloudflare.com/sign-up (ฟรี ไม่ต้องใส่บัตร)
2. เมนูซ้าย → **Workers & Pages** → **Create** → แท็บ **Pages** → **Connect to Git**
3. เชื่อม GitHub แล้วเลือก repo `ArtyZis/gym-tracker`
4. ตั้งค่า build ตามนี้ **เป๊ะๆ**:

   | ช่อง | ใส่ |
   |---|---|
   | Project name | `rankforge-me` (หรือชื่ออื่นที่ชอบ) |
   | Production branch | `main` |
   | Build command | `npm run build` |
   | Build output directory | `dist` |

5. กด **Environment variables** → เพิ่ม `NODE_VERSION` = `24`
6. **Save and Deploy**

รอ 2-3 นาที จะได้ URL หน้าตา `https://rankforge-me.pages.dev`

> **สำคัญ:** ต้องเป็น `npm run build` (ไม่ใช่ `build:pro`) และ output เป็น `dist`
> ถ้าใส่ `dist-pro` จะได้รุ่นที่ขายซึ่งมีช่วงทดลอง 30 วัน — ผิดตัว

### 3. นำข้อมูลเข้าเครื่องใหม่

เปิด URL ใหม่ → แท็บ **จัดการ** → การ์ด **"ย้ายข้อมูลข้ามเครื่อง"**
→ วางโค้ดจากขั้นตอนที่ 1 ลงในช่องข้อความ → กดปุ่มสีฟ้าด้านล่างเพื่อกู้คืน

ถ้าขึ้น "โค้ดไม่ถูกต้อง" แปลว่าโค้ดขาดตอนตอนก๊อป ให้กลับไปทำขั้นตอนที่ 1 ใหม่

### 4. เช็คให้ครบก่อนไปต่อ

- [ ] ประวัติการฝึกครบ (ดูแท็บ ก้าวหน้า → heatmap ย้อนหลัง)
- [ ] สตรีคเท่าเดิม
- [ ] สถิติสูงสุดครบ
- [ ] น้ำหนักตัว/แรงค์ถูก
- [ ] ท้ายหน้าจัดการขึ้นเลขเวอร์ชันตรงกับที่ deploy ล่าสุด

### 5. ติดตั้งลงหน้าจอโฮมใหม่

ลบไอคอนเก่าทิ้ง แล้วเพิ่มไอคอนจาก URL ใหม่แทน
(ไอคอนเก่าชี้ไปที่ `/me/` ซึ่งกำลังจะถูกปิด)

### 6. บอกให้ปิด `/me/`

เมื่อข้อ 4 ครบทุกข้อแล้ว บอกได้เลย จะแก้ `.github/workflows/deploy.yml`
ให้ deploy เฉพาะรุ่นที่ขาย — เว็บที่ขายจะไม่มีรุ่นปลดล็อกซ่อนอยู่ใต้อีก

---

## หลังย้ายเสร็จจะเป็นแบบนี้

| | URL | รุ่น | ข้อมูล |
|---|---|---|---|
| ที่ขาย | artyzis.github.io/gym-tracker/ | pro (ทดลอง 30 วัน) | แยกของตัวเอง |
| ของคุณ | rankforge-me.pages.dev | personal (เปิดหมด) | แยกของตัวเอง |

ทั้งสองที่ deploy อัตโนมัติจาก `main` เหมือนกัน push ครั้งเดียวขึ้นทั้งคู่

**ข้อดีที่ได้เพิ่ม:** Cloudflare ตั้ง security header ได้ (`public/_headers`)
ซึ่ง GitHub Pages ทำไม่ได้ — รุ่นส่วนตัวจะได้ CSP กัน XSS ครบกว่าเว็บที่ขายด้วยซ้ำ
